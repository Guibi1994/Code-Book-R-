---
title: "Algunas pruebas en Rmarkdown"
author: "Guibor Camargo"
date: '2022-06-29'
output: github_document
---

# Esto quiza no se ha puteado!

*R* a veces jode un monton, que fastidio

$$
Dist_i = \alpha+\beta(Speed_i)+\epsilon_i 
$$

```{r grafica_inicial, echo=FALSE,message=FALSE, warning=FALSE, fig.align='center'}
library(dplyr)
library(ggplot2)


cars %>% ggplot(aes(speed, dist))+
  geom_point(color = "cyan4", aplha = 0.4)+
  geom_smooth(color = "orange1")+
  labs(title = "Relación entre distancia y velocidad",
       subtitle = "Pruebas iniciales")+
  theme_minimal()+
  theme(text = element_text(family = "serif"),
        plot.title = element_text(hjust = 0.5),
        plot.subtitle = element_text(hjust = 0.5))
```

```{r}
2+3
```
